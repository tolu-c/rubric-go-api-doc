info:
  name: refresh-access-token
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/auth/refresh"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
