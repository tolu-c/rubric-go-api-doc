info:
  name: change_password
  type: http
  seq: 4

http:
  method: POST
  url: "{{host}}/auth/change-password"
  body:
    type: json
    data: |-
      {
        "new_password": "A!s@D#f$:0192"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
