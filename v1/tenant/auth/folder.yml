info:
  name: auth
  type: folder
  seq: 2

request:
  headers:
    - name: X-School-Slug
      value: "{{schoolSlug}}"
      disabled: true
  auth: inherit
  variables:
    - name: auth
      value: "{{v1}}/auth"
    - name: schoolSlug
      value: marade
      disabled: true
