info:
  name: refresh access token
  type: http
  seq: 6

http:
  method: POST
  url: "{{auth}}/refresh"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
