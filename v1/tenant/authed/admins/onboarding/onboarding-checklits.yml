info:
  name: onboarding-checklits
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/checklist"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
