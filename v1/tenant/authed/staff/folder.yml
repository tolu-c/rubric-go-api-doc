info:
  name: staff
  type: folder
  seq: 3

request:
  headers:
    - name: staffHost
      value: "{{v1}}/staff"
      disabled: true
  auth: inherit
  variables:
    - name: host
      value: "{{v1}}/staff"
