info:
  name: read single notification
  type: http
  seq: 4

http:
  method: POST
  url: "{{host}}/:id/read"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
